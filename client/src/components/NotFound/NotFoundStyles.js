import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  title: {
    justifyContent: 'center',
    color:'#fff',
    fontWeight: '900',
    fontSize: '5rem',
    height: '90%',
    width: '100%'
  }
}));